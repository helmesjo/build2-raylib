# libraylib-examples

This is a `build2` package for the official [raylib](https://www.raylib.com) examples.

Examples are interactive GUI demos and are not run as automated tests. They are
gated on the same module configuration flags as `libraylib` so only examples
whose required modules are enabled are included in the build.
