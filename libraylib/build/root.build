# Suppress warnings coming from vendored third-party headers (GLFW, stb, etc.).
#
c.internal.scope = current

using c

h{*}: extension = h
c{*}: extension = c

# The test target for cross-testing (running tests under Wine, etc).
#
test.target = $c.target
